public class MemoryManagement {
    public static void main(String[] args) {
        /*
        processes vs threads
        threads of the same process run in a shared memory space

        stack memory - local variables are stored on stack memory
        heap memory  - objects live on heap memory

        every thread has its own stack memory but all threads
        share the heap memory

        the main purpose of synchronization is the sharing
        of resources without interference using mutual exclusion

         */
    }
}
