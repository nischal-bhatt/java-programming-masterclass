public class ThreadLifeCycle {
    public static void main(String[] args) {
        /*
        new - in this state until we call start() method
        runnable - after we have started the thread
        waiting - waiting for other threads to finish its task
        dead - after the thread finishes its task

         */
    }
}
